VITE_API_URL=http://localhost:8000
// Inside a component or fetch request
const response = await fetch(`${import.meta.env.VITE_API_URL}/login`, {
  method: 'POST',
  body: JSON.stringify({ email, password })
});
